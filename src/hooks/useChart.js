import { useState } from "react"


const useChart = () => {
    const [data, setData] = useState({
      labels: [],
      datasets: []
    })

    const toChartFormat = (unformatedData, yField, xField) => {

    }
    return (
      toChartFormat
    )
}

export default useChart